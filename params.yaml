# Experiment parameters for T-Maze DVC pipeline
experiment:
  n_episodes: 1000
  max_steps: 4
  planning_horizon: 4
  receding_horizon: true
  n_optimization_steps: 120
  learning_rate: 0.1
  seed: 42
